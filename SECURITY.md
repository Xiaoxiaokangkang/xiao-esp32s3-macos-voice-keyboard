# 安全与权限说明

## Mac 安装脚本会执行的操作

- 将一个或两个 App 复制到 `/Applications`。
- 在当前用户的 `~/Library/LaunchAgents` 中安装登录启动配置。
- 使用 `launchctl` 启动后台程序。
- 移除所安装 App 的 `com.apple.quarantine` 扩展属性。
- 打开系统“隐私与安全性”设置页。

执行前建议先在文本编辑器中查看 `.command` 文件。安装过程可能调用 `sudo`，管理员密码只由 macOS 自己读取，本项目脚本不会保存密码。

## 辅助功能权限

V2/V3 的 Fn Bridge 用于键位映射和音频输入设备切换。V4 的 TraeWork Bridge 需要辅助功能权限，以便激活 TRAE SOLO CN 并定位其输入区域。

辅助功能属于高权限能力。只应从可信来源下载程序，并使用 `SHA256SUMS.txt` 校验文件。若不再使用本项目，应删除对应 App 和 LaunchAgent，并在系统设置中撤销辅助功能权限。

## 代码与隐私

- 本公开仓库包含经过脱敏检查的源码和构建工程，但不包含签名私钥、密码、Token、个人日志、私有设备 MAC 地址或原设备 USB 序列号。
- 桥接程序会在 macOS 临时目录写入诊断日志，日志用于记录设备连接和程序状态，不应包含输入文本内容。
- 本项目没有联网服务、云端账号或 API Token 配置。
- 源码中的 Bundle ID、目标应用 Bundle ID、USB VendorID/ProductID 和临时日志文件名属于程序运行配置，不是账号凭据。
- V1 的历史完整 Flash 含旧编译环境路径字符串，且不具备与源码构建版本相同的隐私可审计性，因此不在公开仓库中发布。

## 报告问题

公开报告问题时，请不要上传管理员密码、个人路径、设备序列号、聊天内容或完整系统日志。建议只提供所用版本、macOS 版本、Mac 芯片类型、硬件接线和错误提示。
