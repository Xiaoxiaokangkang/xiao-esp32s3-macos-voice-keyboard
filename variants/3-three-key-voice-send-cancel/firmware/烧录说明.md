# V3 固件烧录

推荐运行 `烧录V3.command`，或将 `XIAO-ESP32S3-Voice-Keyboard-V3-complete.bin` 写入 ESP32-S3 的 `0x0`。

分立文件地址：

- `0x0000` → `bootloader.bin`
- `0x8000` → `partitions.bin`
- `0xE000` → `boot_app0.bin`
- `0x10000` → `firmware-app-V3.bin`
